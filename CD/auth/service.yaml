apiVersion: v1
kind: Service
metadata:
  name: auth-frontend-service-svc-dp
  namespace: dev
spec:
  type: NodePort
  selector:
    app: auth-frontend-service-app
  ports:
    - port: 5173
      targetPort: 80
      protocol: TCP
      nodePort: 30006
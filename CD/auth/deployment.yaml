apiVersion: apps/v1
kind: Deployment

metadata:
  name: auth-frontend-service
  namespace:  dev

spec:
  minReadySeconds: 3
  replicas: 2

  selector:
    matchLabels:
      app: auth-frontend-service-app
  
  strategy:
    type: RollingUpdate

    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  
  template:
    metadata:
      name: auth-frontend-service-pod
      labels:
        app: auth-frontend-service-app
    
    spec:
      containers:
        - name: auth-frontend-service
          image:  
          envFrom:
            - configMapRef:
              name: auth-frontend-service-config

          imagePullPolicy:  Always

          readinessProbe:
            httpGet:
              path: /
              ports: 5173

          ports:
            - containerPort:  5173
              protocol: TCP

          resources:
            requests:
              cpu: "50m"
              memory: "80Mi"
            
            limits:
              cpu: "80m"
              memory: "100Mi"
apiVersion: v1
kind: Service
metadata:
  name: order-frontend-service-svc-dp
  namespace: dev
spec:
  type: NodePort
  selector:
    app: order-frontend-service-app
  port:
    - port: 5177
      targetPort: 5177
      protocol: TCP
      nodePort: 30009
apiVersion: apps/v1
kind: Deployment

metadata:
  name: profile-frontend-service
  namespace: dev

spec:
  minReadySeconds: 3
  replicas: 2

  selector:
    matchLabels:
      app: profile-frontend-service-app
  
  strategy:
    type: RollingUpdate

    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  
  template:
    metadata:
      name: profile-frontend-service-pod
      labels:
        app: profile-frontend-service-app
    
    spec:
      containers:
        - name: profile-frontend-service
          image:  
          envFrom:
            - configMapRef:
              name: profile-frontend-service-config

          imagePullPolicy:  Always

          readinessProbe:
            httpGet:
              path: /
              ports: 5178

          ports:
            - containerPort:  5178
              protocol: TCP

          resources:
            requests:
              cpu: "50m"
              memory: "80Mi"
            
            limits:
              cpu: "80m"
              memory: "100Mi"